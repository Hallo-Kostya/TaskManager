<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:viewmodels="clr-namespace:App1.ViewModels" 
             x:DataType="viewmodels:FolderAddingViewModel"
             x:Class="App1.Views.FolderAddingPage"
             BackgroundColor="Black">
    
    <ContentPage.Content>
        <StackLayout Padding="20" >
            <Frame CornerRadius="30" BackgroundColor="#252525" >
                <StackLayout>
                    <Label Text="Введите название папки:" FontSize="16" TextColor="AliceBlue" FontAttributes="Bold" />
                    <StackLayout Orientation="Horizontal">
                        <Image Source="align.png"/>
                        <Entry Placeholder="Папка" x:Name="EntryFolderName" PlaceholderColor="Gray" Text="{Binding WritenName,Mode=TwoWay}" TextColor="AliceBlue" HorizontalOptions="FillAndExpand"/>
                    </StackLayout>
                    <Button Command="{Binding AddNewFolderCommand}" Text="Создать" TextColor="AliceBlue" BackgroundColor="Gray" CornerRadius="20" Margin="0,20"/>
                </StackLayout>
            </Frame>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>